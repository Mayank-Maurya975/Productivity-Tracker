import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mayank.tracker',
  appName: 'Productivity Tracker',
  webDir: 'dist'
};

export default config;
